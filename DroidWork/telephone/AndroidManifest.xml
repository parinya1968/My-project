<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="telephone.sqlite.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".TelephoneActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
        </activity>
        <activity 
            android:name=".showName"
            android:label="รายชื่อ"/>
        <activity 
            android:name=".addName"
            android:label="@string/app_name" />
        <activity 
            android:name=".ShowName"
            android:label="รายชื่อ" />
        <activity 
            android:name=".showMsg"
            android:label="@string/srtError"
            android:theme="@android:style/Theme.Dialog" />
        <activity 
            android:name=".ShowData"
            android:label="ข้อมูล"
            android:theme="@android:style/Theme.Dialog" />
        
    </application>

</manifest>